{
  "name": "app",
  "version": "1.0.0",
  "description": "App side (checking the database, and sending notifications)",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Abdullah Kady",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^5.0.1",
    "mongodb": "^3.0.4",
    "nodemailer": "^4.6.3",
    "nodemailer-sendgrid-transport": "^0.2.0",
    "request": "^2.85.0"
  }
}
